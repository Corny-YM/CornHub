export enum StatusRelationEnum {
  single = "single",
  dating = "dating",
  marriage = "marriage",
}

export enum StatusGenderEnum {
  male = "1",
  female = "0",
}

export enum TypeFileEnum {
  image = "image",
  video = "video",
}

export enum UsedForEnum {
  post = "post",
  avatar = "avatar",
  cover = "cover",
  comment = "comment",
  reply = "reply",
  message = "message",
}

export enum TypeConversationEnum {
  group = "group",
  private = "private",
}

export enum TypeMessageEnum {
  message = "message",
  file = "file",
}

export enum OptionDeleteMessageEnum {
  recall = "recall",
  terminate = "terminate",
}
